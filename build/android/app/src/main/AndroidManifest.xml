<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Solo lo que Kaname usa: la base está en la red, y la biometría
         protege la clave del Keystore. La plantilla de Wails pide además
         cámara, ubicación, notificaciones y servicio en primer plano; ninguno
         tiene un uso acá y un permiso que no se usa es superficie de más. -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.USE_BIOMETRIC" />

    <!-- allowBackup=false: la libreta no tiene secretos, pero el estado de la
         app no tiene por qué viajar a la nube de Google por su cuenta. -->
    <!-- KanameApp es propia: le da al vault de contraseñas el Context y la
         activity en primer plano. MainActivity y el bridge son los de Wails. -->
    <application
        android:name="dev.kaname.vault.KanameApp"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.WailsApp"
        tools:targetApi="31">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:configChanges="orientation|screenSize|keyboardHidden|uiMode"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
